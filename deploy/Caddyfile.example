# Reverse proxy with automatic HTTPS (Let's Encrypt).
# Replace checker.yourdomain.com with your real domain (A record -> VPS IP),
# copy to /etc/caddy/Caddyfile, then: sudo systemctl reload caddy
checker.yourdomain.com {
    reverse_proxy localhost:3000
    encode gzip
}
